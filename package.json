{
  "name": "claude-cron-starter",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun src/cron.ts",
    "watch": "bun --watch src/cron.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "biome check src tests",
    "fmt": "biome format --write src tests",
    "test": "bun test"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.198",
    "croner": "^10.0.1"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.2",
    "@types/bun": "^1.3.14",
    "@types/node": "^26.1.0",
    "typescript": "^6.0.3"
  }
}
